<% if user_signed_in? %>
<h1>New Image</h1>

<%= form_for :album_image, :html => { :multipart => true }, url: album_images_path do |f| %>
<% if @image.errors.any? %>
<div id="error_explanation">
<h2>
	<%= pluralize(@image.errors.count,"errors") %> prohibited this image from being saved:
</h2>
<ul>
	<% @image.errors.full_messages.each do |msg| %>
	<li><%= msg %></li>
	<% end %>
</ul>
</div>
<% end %>
	<p>upload an image : </p>
	<p>
		<%= f.file_field :picture %>
	</p>
	<p>
	<%= f.submit %>
	</p>
<% end %>
<%= link_to 'Back', album_images_path %>
<% else %>
<%= link_to "Sign In", new_user_session_path, :method => :get %>
<%= link_to "Sign up", new_user_registration_path, :method => :get %>

<% end %>